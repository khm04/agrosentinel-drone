{
  "firestore": {
    "rules": "firestore.rules"
  },
  "storage": [
    {
      "target": "default",
      "rules": "storage.rules"
    },
    {
      "target": "agrosentinel",
      "rules": "storage.rules"
    }
  ]
}
